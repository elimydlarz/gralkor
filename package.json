{
  "name": "@susu-eng/gralkor",
  "version": "26.0.17",
  "description": "OpenClaw memory plugin powered by Graphiti knowledge graphs and FalkorDB",
  "type": "module",
  "main": "./dist/index.js",
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "pnpm run typecheck && pnpm run test:plugin && pnpm run test:functional && pnpm run test:server",
    "test:plugin": "vitest run",
    "test:functional": "vitest run --config test/functional/vitest.config.ts",
    "test:server": "cd server && uv run pytest tests/",
    "test:server:changed": "cd server && files=$(git diff --name-only --diff-filter=d HEAD -- 'tests/*.py') && [ -n \"$files\" ] && uv run pytest $files || echo 'No changed server test files'",
    "test:mutate": "stryker run",
    "setup:server": "cd server && uv sync",
    "pack": "bash scripts/pack.sh",
    "docker:build": "docker build -t gralkor-server:latest server/",
    "docker:up": "pnpm run docker:build && docker compose up -d",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs graphiti",
    "harness": "bash test/harness/build.sh",
    "harness:npm": "bash test/harness/build.sh --npm",
    "harness:run": "docker run --rm -it --platform linux/amd64 gralkor-harness:latest",
    "harness:shell": "docker run --rm -it --platform linux/amd64 gralkor-harness:latest bash",
    "publish:npm": "bash scripts/publish.sh"
  },
  "files": [
    "dist",
    "server/Dockerfile",
    "server/main.py",
    "server/pyproject.toml",
    "server/uv.lock",
    "server/requirements.txt",
    "server/wheels",
    "openclaw.plugin.json",
    "docker-compose.yml",
    "config.yaml",
    ".env.example"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/susu-eng/gralkor.git"
  },
  "homepage": "https://github.com/susu-eng/gralkor#readme",
  "bugs": {
    "url": "https://github.com/susu-eng/gralkor/issues"
  },
  "author": "susu-eng",
  "keywords": [
    "openclaw",
    "openclaw-plugin",
    "graphiti",
    "falkordb",
    "knowledge-graph",
    "memory",
    "ai-agent"
  ],
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18"
  },
  "peerDependencies": {
    "openclaw": ">= 2026.1.26"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.2"
  },
  "license": "MIT"
}
